class HomeSlider {
  const HomeSlider({
    required this.id,
    required this.title,
    required this.subtitle,
    required this.imgUrl,
    required this.url,
  });

  final String id, title, subtitle, imgUrl, url;
}
