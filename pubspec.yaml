name: iroyal
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  crypto: ^3.0.3
  cupertino_icons: ^1.0.6
  encrypt: ^5.0.3
  flutter:
    sdk: flutter
  get: ^4.6.6
  intl: ^0.19.0
  tuple: ^2.0.2

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
